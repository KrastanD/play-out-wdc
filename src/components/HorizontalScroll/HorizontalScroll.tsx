import "./HorizontalScroll.styles.scss";

function HorizontalScroll({ children }: { children: React.ReactNode }) {
  return <div className="HorizontalScroll">{children}</div>;
}

export default HorizontalScroll;
